/**
 * Re-export canonical SourceType from @apex/contracts.
 * ai-core domain uses the same canonical type — no duplication.
 */
export type { SourceType } from '@apex/contracts'
export { ALL_SOURCE_TYPES, SOURCE_LABELS } from '@apex/contracts'
